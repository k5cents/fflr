% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/id.R
\name{ffl_cookie}
\alias{ffl_cookie}
\title{Get ESPN authentication cookie}
\usage{
ffl_cookie(cookie = getOption("fflr.cookie"))
}
\arguments{
\item{cookie}{The \code{espn_s2} string. Defaults to \code{getOption("fflr.cookie")}
and then \code{Sys.getenv("ESPN_S2")}.}
}
\value{
The cookie as a character string, or \code{NULL} if none is set.
}
\description{
Retrieve the \code{espn_s2} cookie from global options or an environment variable.
}
\details{
The \code{leagueHistory} endpoint requires a cookie from a signed-in ESPN session.
Rather than passing it to every call, the cookie can be set once as the
\code{fflr.cookie} option or the \code{ESPN_S2} environment variable. The cookie is
found in your browser's developer tools after signing into ESPN and should be
copied exactly, including any percent-encoded characters.
}
\examples{
ffl_cookie("abc123")
}
