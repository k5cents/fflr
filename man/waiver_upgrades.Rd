% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waiver.R
\name{waiver_upgrades}
\alias{waiver_upgrades}
\title{Find waiver wire upgrades}
\usage{
waiver_upgrades(
  leagueId = ffl_id(),
  teamId,
  seasonId = ffl_year(),
  scoringPeriodId = ffl_week(),
  useScore = c("projectedScore", "actualScore"),
  limit = 10,
  cookie = ffl_cookie()
)
}
\arguments{
\item{leagueId}{Numeric league ID or ESPN fantasy page URL. Defaults to
\code{getOption("fflr.leagueId")}. Function fails if no ID is found.}

\item{teamId}{The team ID looking for upgrades (see \code{\link[=league_teams]{league_teams()}}).}

\item{seasonId}{Integer year of the NFL season. Defaults to the current
season (see \code{\link[=ffl_year]{ffl_year()}}) rather than \code{\link[=ffl_api]{ffl_api()}}'s fixed default, which
is only right for the year of the fflr release.}

\item{scoringPeriodId}{Integer vector of one or more weeks to score, or
"rest" for the rest of the regular season. Defaults to the current week
(see \code{\link[=ffl_week]{ffl_week()}}).}

\item{useScore}{One of "projectedScore" (default) or "actualScore".}

\item{limit}{The number of available players to consider at each
position, most rostered first. Defaults to 10.}

\item{cookie}{The alphanumeric \code{espn_s2} cookie string from a signed-in
session, defaulting to \code{\link[=ffl_cookie]{ffl_cookie()}}. Since 2025, this cookie is required
by the \code{leagueHistory} endpoint. Past seasons can also be requested without
a cookie by passing an explicit \code{seasonId} instead of using
\code{leagueHistory = TRUE}.}
}
\value{
A tibble with one row per add that improves the lineup, best
first: the player to add and their \code{status} ("FREEAGENT" or "WAIVERS"),
the player to drop (\code{NA} with an open roster spot), the optimal starting
score summed over the scored weeks without the move (\code{scoreBefore}) and
with it (\code{scoreAfter}), their difference (\code{delta}), the number of weeks
the move helps (\code{weeksImproved}), and the players the add takes a
starting spot from in any of those weeks (\code{startersOut}). Player lists
are comma-separated names, \code{NA} when empty; no rows if nothing helps.
}
\description{
Score every add/drop a team could make from the waiver wire: for each of
the most-rostered available players at each starting position, the best
player to drop for them and how much the team's optimal starting lineup
gains, week by week, using the league's own lineup optimizer (see
\code{\link[=best_roster]{best_roster()}}) and ESPN's per-week projections. The pickup counterpart to
\code{\link[=evaluate_trade]{evaluate_trade()}}.
}
\details{
The pool is today's free agents and players on waivers (see \code{status} in
the output), the top \code{limit} at each single-position starting slot by
percent rostered -- the same pool \code{\link[=evaluate_trade]{evaluate_trade()}} and \code{\link[=best_roster]{best_roster()}}
take their replacement-level stand-ins from. It's today's pool, not a
forecast of who will be available in later weeks.

If the roster has an open spot (not counting IR), nobody is dropped.
Otherwise each add is paired with the drop that costs the least: a player
who wouldn't start in any scored week after the add costs nothing, and of
those, the one with the lowest total score is chosen; if every player
would start at some point, each is tried. Players on IR are never
dropped, since dropping them frees no roster spot.
}
\examples{
\dontrun{
waiver_upgrades(teamId = 6)
waiver_upgrades(teamId = 6, scoringPeriodId = "rest")
}
}
\seealso{
Other roster functions:
\code{\link[=best_roster]{best_roster()}},
\code{\link[=evaluate_trade]{evaluate_trade()}},
\code{\link[=roster_score]{roster_score()}},
\code{\link[=start_roster]{start_roster()}},
\code{\link[=team_roster]{team_roster()}}
}
\concept{roster functions}
