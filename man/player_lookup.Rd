% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/players.R
\name{player_lookup}
\alias{player_lookup}
\title{Look up players by ID}
\usage{
player_lookup(
  playerId,
  leagueId = ffl_id(),
  seasonId = ffl_year(),
  scoringPeriodId = ffl_week(),
  cookie = ffl_cookie()
)
}
\arguments{
\item{playerId}{Integer vector of player IDs. Defense IDs are negative. An
error is raised if any ID doesn't match a player.}

\item{leagueId}{Numeric league ID or ESPN fantasy page URL. Defaults to
\code{getOption("fflr.leagueId")}. Function fails if no ID is found.}

\item{seasonId}{Integer year of NFL season. By default, the season is
currently set to 2026. Use a recent year or set \code{leagueHistory} to \code{TRUE}
to obtain all past data.}

\item{scoringPeriodId}{Integer week of NFL season. By default, \code{NULL} will
use the current week (see \code{\link[=ffl_week]{ffl_week()}}). Scoring periods are always one
week in length, whereas matchups might be longer.}

\item{cookie}{The alphanumeric \code{espn_s2} cookie string from a signed-in
session, defaulting to \code{\link[=ffl_cookie]{ffl_cookie()}}. Since 2025, this cookie is required
by the \code{leagueHistory} endpoint. Past seasons can also be requested without
a cookie by passing an explicit \code{seasonId} instead of using
\code{leagueHistory = TRUE}.}
}
\value{
A tibble, one row per unique \code{playerId} in the order given, shaped
like a \code{\link[=team_roster]{team_roster()}} row but without \code{teamId}, \code{abbrev}, or \code{lineupSlot} -- a looked-up
player isn't rostered by, or slotted on, any one team.
}
\description{
Player detail for any player ID in the league -- rostered, free agent, or
on waivers, including defenses -- shaped like a row from \code{\link[=team_roster]{team_roster()}}.
}
\details{
\code{\link[=player_info]{player_info()}} can't do this: it 404s on defenses, whose IDs are
negative, and its upstream source drops fields inconsistently. This asks
the same \code{kona_player_info} view \code{\link[=list_players]{list_players()}} uses, filtered to
exactly these IDs with an \code{X-Fantasy-Filter} header, so it works for any
player regardless of which team, if any, rosters them -- the building
block for simulating a roster that doesn't exist yet, as \code{\link[=evaluate_trade]{evaluate_trade()}}
does.
}
\examples{
\dontrun{
player_lookup(playerId = c(4427366, -16027))
}
}
\seealso{
Other player functions:
\code{\link[=all_players]{all_players()}},
\code{\link[=list_players]{list_players()}},
\code{\link[=player_info]{player_info()}},
\code{\link[=player_news]{player_news()}},
\code{\link[=player_outlook]{player_outlook()}},
\code{\link[=recent_activity]{recent_activity()}},
\code{\link[=transaction_counter]{transaction_counter()}}
}
\concept{player functions}
